<!--index.wxml-->
<view class="container page">
  <!-- <view class="userinfo">
    <button  open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber"> 获取手机号码 </button>
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view> -->
  <view class="user-center">
    <image class="bg-image" src="../../images/index-bg.png"></image>
    <view>
      <image class="avatar" src="{{userInfo.image}}"></image>
      <text>{{userInfo.name}}</text>
    </view>
  </view>
  <van-button class="logout-btn" size="large" bindtap="logout" form-type="submit" type="danger" wx:if="{{userInfo.name}}"> 退出登录 </van-button>
  <van-button style="width: 100%;" size="large" bindtap="toPage" data-path="login" type="primary" plain wx:else> 去登录 </van-button>
</view>
